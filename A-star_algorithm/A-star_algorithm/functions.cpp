#include <iostream>
#include <list>
#include <vector>

#include "functions.h"

using namespace std;

list<pair<int, int>> A_star_2D(){
	vector<pair<int, int>> open, closed;
	bool serching = true;
	do
	{

	} while (serching);

	list < pair<int, int >> path;
	return path;
}